/*
 * @lc app=leetcode.cn id=98 lang=cpp
 *
 * [98] 验证二叉搜索树
 *
 * https://leetcode-cn.com/problems/validate-binary-search-tree/description/
 *
 * algorithms
 * Medium (24.82%)
 * Total Accepted:    17.2K
 * Total Submissions: 69.4K
 * Testcase Example:  '[2,1,3]'
 *
 * 给定一个二叉树，判断其是否是一个有效的二叉搜索树。
 * 
 * 假设一个二叉搜索树具有如下特征：
 * 
 * 
 * 节点的左子树只包含小于当前节点的数。
 * 节点的右子树只包含大于当前节点的数。
 * 所有左子树和右子树自身必须也是二叉搜索树。
 * 
 * 
 * 示例 1:
 * 
 * 输入:
 * ⁠   2
 * ⁠  / \
 * ⁠ 1   3
 * 输出: true
 * 
 * 
 * 示例 2:
 * 
 * 输入:
 * ⁠   5
 * ⁠  / \
 * ⁠ 1   4
 * / \
 * 3   6
 * 输出: false
 * 解释: 输入为: [5,1,4,null,null,3,6]。
 * 根节点的值为 5 ，但是其右子节点值为 4 。
 * 
 * 
 */
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     TreeNode *left;
 *     TreeNode *right;
 *     TreeNode(int x) : val(x), left(NULL), right(NULL) {}
 * };
 */
class Solution {
public:
    bool isValidBST(TreeNode* root) {
        return isValidBSTHelp(root, long(INT_MIN) - 1, long(INT_MAX) + 1);
    }
    
    bool isValidBSTHelp(TreeNode* root, long min, long max) {
        if(!root)
            return true;
        bool leftRes = true, rightRes = true;
        long val = (long)root->val;
        bool selfRes = val > min && val < max;
        if(root->left)
            leftRes = root->left->val < val && isValidBSTHelp(root->left, min, std::min(max, val));
        if(root->right)
            rightRes = root->right->val > val && isValidBSTHelp(root->right, std::max(min, val), max);
        return leftRes && rightRes && selfRes;
    }
};

